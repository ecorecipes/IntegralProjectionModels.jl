# Domains

Domain types define the state space over which IPM kernels operate. A `ContinuousDomain` discretizes a continuous interval into mesh points, while a `DiscreteDomain` represents a finite set of discrete states.

```@docs
ContinuousDomain
DiscreteDomain
meshpoints
step_size
bounds
n_states
```
